import React from 'react';
import { render, waitFor } from "test-utils";
import { toJSON } from "react-native";

import BarCodeScanComponent from '../../components/BarCodeScanComponent';

describe('BarCodeScanComponent', () => {
  test('renders correctly', async () => {

    // incomplete, requires some mocks
    // const screen = render(<BarCodeScanComponent />);

    // await waitFor(async () => {    
    //     const tree = toJSON(screen);
    //     expect(tree).toMatchSnapshot();
    // });
   
  });
});

// describe("HBarCodeScanComponent", () => {

// });